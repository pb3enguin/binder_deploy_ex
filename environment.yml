name: torch
channels:
  # - defaults
  - pytorch
  - conda-forge
dependencies:
  - python=3.11
  - ipykernel
  - pillow
  - numpy
  - pytorch=2.5.1
  # - torchvision
  - cpuonly
prefix: /opt/anaconda3/envs/torch
